# NotebookLM project isolation plan

Status: compatibility pointer. The authoritative project context retrieval and
NotebookLM isolation contract now lives in
`docs/PROJECT_CONTEXT_RETRIEVAL_PROTOCOL.md`.

This file is intentionally short so it does not duplicate the full contract. It
is kept for older links that previously opened the isolation plan.

Open the unified document for:

- project→canonical NotebookLM ownership and pending confirmations;
- fail-closed `project_id` and canonical-only write policy;
- explicit one-hop cross-project dependency-card limits;
- migration phases, acceptance tests, rollback and governance.

Compatibility note: no notebooks, notes, runtime, prompts, providers,
deployments or production state are changed by this pointer.
