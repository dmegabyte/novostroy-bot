# yaml-language-server: $schema=https://promptfoo.dev/config-schema.json
description: "nmbot scenario-suite regression — local preview/no LLM cost"

prompts:
  - file://prompts/answer.md

providers:
  - echo

tests: file://tests/scenario-suite.yaml

defaultTest:
  assert:
    - type: python
      value: file://scripts/answer_quality_assert.py:get_assert
      threshold: 0.75

commandLineOptions:
  maxConcurrency: 1

outputPath: results/scenario-preview-results.json
