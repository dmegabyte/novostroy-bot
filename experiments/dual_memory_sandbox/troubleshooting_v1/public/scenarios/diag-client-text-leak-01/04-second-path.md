# 04 — Second path

Evidence IDs: `leak.e10`, `leak.e11`, `leak.e12`.

- `leak.e10`: A later renderer matrix found another exposure path in current
  options / financing-style answers.
- `leak.e11`: That second path used raw clarification/execution text rather than
  the handoff reason field.
- `leak.e12`: The key diagnostic point is two presentation paths allowed internal
  planner/state markers to become client text; neither artifact requires claiming
  MCP/search failure.
