# STOP-2 bounded retrieval protocol

Status: compatibility pointer. The authoritative STOP-2, route-selection,
machine-trace and acceptance-test contract now lives in
`docs/PROJECT_CONTEXT_RETRIEVAL_PROTOCOL.md`.

This file is intentionally short so it does not duplicate the full contract. It
is kept for older links that previously opened the bounded retrieval draft.

Open the unified document for:

- Stage 0 retrieval contract fields;
- route decision table and select-before-read rule;
- STOP-2 limits and their benchmark-hypothesis boundary;
- expansion gate, mandatory stop reasons and topic-drift guard;
- `bounded-retrieval.v1` trace and denial reasons.

Compatibility note: no retrieval tooling, runtime, prompts, providers,
deployments or production state are changed by this pointer.
