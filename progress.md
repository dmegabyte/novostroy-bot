# Progress — H026 Ideal Irina UX

## 2026-07-03 — Public history tab

- User asked to add a `История` tab to `http://193.107.155.236:8765/nmbot-project-7f3a9c/index.html` so latest bot dialogs can be viewed online.
- Added `scripts/publish_public_history.py`: reads `logs/dialogs-YYYY-MM-DD.jsonl`, publishes sanitized `history.json`, masks phones/emails/tokens, truncates long MCP/search fields.
- Updated `scripts/build_public_overview.py`: NMBOT overview now has nav link `История`, section `История последних диалогов`, auto-refresh fetch of `history.json` every 10 seconds, and manual refresh button.
- Deployed static page to existing `/home/neiro/public` service and installed user timer `nmbot-public-history.timer` to refresh history JSON every 10 seconds.
- Verification: local `LOCAL_HISTORY_OVERVIEW_OK`; remote `REMOTE_HISTORY_TAB_OK 30`; webfetch confirms public page contains `История` and public `history.json` returns sanitized recent items.

## 2026-07-03 — Mandatory MCP/search rule

- User pointed out a critical rule from the earlier search bot: for any apartment request, the bot must call `get_flat_info`; it cannot answer without the tool.
- Mapped this to current nmbot contract: any apartment / new-build / ЖК / подбор request must trigger MCP/search (`novostroym`) before Irina answers with factual variants.
- Updated `prompts/search_v1.txt` with a hard tool rule: no model-memory answers; prices, areas, deadlines, finishing, infrastructure and variants must come from MCP/search; incomplete requests still run search and return missing/params.
- Updated `docs/BOT_ARCHITECTURE.md`, `docs/BOT_SCENARIO_MAP.html`, and `scripts/build_public_overview.py` so the public overview and scenario map show the mandatory MCP/search step.
- Verification/deploy/git still pending after this note.

## 2026-06-26

- Started H026 after user requested best UX and strict dialog evaluation.
- User added hard rule: no hallucinations, only information received from MCP.
- Created target docs and dialog review journal.
- First H026 dialog review passed basic checks, but manual strict UX inspection found a weak question: “Какой бюджет и тип отделки вас интересует?” asks two axes at once. Tightened the gate so multi-axis questions fail.
- Second H026 run produced a better one-step question, but still used long dash `—` inside numbered options. Tightened the gate so dash/bullet lists fail.
- User rejected process-only healthcheck: green PID/getUpdates is not enough. Tightened `scripts/nmbot_test_agent.py` so default `suite=all` includes deploy-smoke plus live dialog gate.
- Expanded live dialog gate from 1 to 3 scenarios and added system observability checks: params parsing, MCP/search JSON presence/validity, facts/near counts, expected context, backend meta error markers, and sanitized meta in `logs/dialog_reviews.md`.
- Fixed stale-budget detector so it catches real old `5 млн` context without false-positive on prices like `15.49 млн`.
- Fixed `prompts/chat_v1.txt` few-shot and hard rule: no combined final questions like “какой район или бюджет”.
- Verification: `python3 scripts/nmbot_test_agent.py` now passes 22/22, including DEPLOY 1/1 and DIALOG 3/3.
- After Telegram handler hang fix and UX hardening, `docs/IDEAL_IRINA_UX.md` is now the single UX North Star for nmbot. Any work touching Irina answers, prompts, Telegram handler, buttons, dialog memory, MCP/search parsing, or tests must be checked against this file before handoff.
- Updated UX target: first useful answer must feel like a calm consultant, not a catalog dump; show maximum 3 options, add short benefit from MCP facts, ask exactly one next-step question, and avoid early operator fallback.
- Updated `AGENTS.md` to require reading `docs/IDEAL_IRINA_UX.md` before UX-affecting changes.
- Verification after UX contract update: `python3 scripts/nmbot_test_agent.py --suite dialog` passed 3/3; full `python3 scripts/nmbot_test_agent.py` passed 23/23 including DEPLOY 1/1 and DIALOG 3/3.

## 2026-06-30

- Added `scripts/nmbot_mcp_only_sim.py` as a read-only MCP-only simulator for UX hypotheses before changing production/dev bot logic.
- User accepted the process: simulate hypothesis first, inspect the dialog, correct the mechanics, then change `chat_tester_bot.py`/prompts/tests.
- Documented this as **Hypothesis Simulation Gate** in `docs/EXPERIMENTS.md` and added the simulator to `AGENTS.md`.
- Applied the first Hypothesis Simulation Gate result: selected ЖК follow-ups now split “расскажи/подробнее” from live-data/operator requests. `подробнее` routes to `explain_selected_option`; `бронь/наличие/этаж/корпус/ипотека` still routes to `operator_for_selected`.
- Updated selected-option card wording so it no longer immediately asks to hand off to operator; it offers to explain the ЖК or compare with other options.
- Verification: `python3 -m py_compile scripts/chat_tester_bot.py scripts/nmbot_test_agent.py`; `python3 scripts/nmbot_test_agent.py --suite ux_e2e --json` 5/5; `python3 scripts/nmbot_test_agent.py --suite h028 --json` 5/5; `python3 scripts/nmbot_test_agent.py --suite h026 --json` 1/1; simulator recheck passed for `1`, `расскажи подробнее`, `1, можно бронь?`.
- Warmed selected-ЖК details at the LLM/style layer: `_build_known_option_prompt()` now teaches Irina to explain selected ЖК without client-visible technical phrases (`MCP`, `JSON`, `не удалось подтвердить`, `чтобы не выдумывать`, `доходность и ликвидность нужно проверять отдельно`) and includes good/bad tone examples. `prompts/text_style_v1.txt` now rewrites cold operator/detail wording into warmer live-consultant language.
- Minimal fallback wording was also softened so simulator and style-tool fallback no longer show “Больше подтверждённой информации прямо сейчас не добавлю, чтобы не выдумывать”.
- Verification after warm-style update: `python3 -m py_compile scripts/chat_tester_bot.py scripts/nmbot_test_agent.py scripts/nmbot_mcp_only_sim.py`; `python3 scripts/nmbot_test_agent.py --suite h029 --json` 14/14; `python3 scripts/nmbot_test_agent.py --suite ux_e2e --json` 5/5; `python3 scripts/nmbot_test_agent.py --suite h028 --json` 5/5; simulator output for `расскажи подробнее` now says “По ЖК «Лучи» картина такая...” and avoids technical MCP/missing-data wording.
- Structured selected-ЖК prompt contract completed: `_build_known_option_prompt()` now uses explicit sections (`ROLE`, `SITUATION`, `USER_ACTION`, `CLIENT_INTENT`, `SAFE_FACTS`, `ALLOWED_INFERENCES`, `MISSING_OR_LIVE_ONLY`, `FORBIDDEN_FACTS`, `FORBIDDEN_PHRASES_FOR_CLIENT`, `RESPONSE_SHAPE`, `STYLE_EXAMPLES`, `OUTPUT_JSON`).
- Fixed contact-accept loop: after Irina asks “Оставите номер для связи?”, short confirmations like `да`/`хочу` route to `operator_contact_accept` and ask for the phone number instead of repeating the ЖК presentation.
- Verification after contact-accept fix: `python3 -m py_compile scripts/chat_tester_bot.py scripts/nmbot_test_agent.py scripts/nmbot_mcp_only_sim.py`; `python3 scripts/nmbot_test_agent.py --suite h029 --json` 14/14; `python3 scripts/nmbot_test_agent.py --suite ux_e2e --json` 6/6; `python3 scripts/nmbot_test_agent.py --suite h028 --json` 5/5; simulator recheck passed for `1`, `расскажи подробнее`, `1, можно бронь?`.
- User preference: after chatbot/live test batches, print OpenRouter cost via `python3 scripts/or_cost.py`; current post-test table: today `$0.98`, week `$2.15`, month `$27.17`, total `$29.60`.
- Implemented negation handling according to `docs/PRODUCT_TZ.md` 5.2.1: explicit refusals like `не хочу оператора` / `не надо звонить` are caught by minimal code safety and route to `reject_operator`; product negations like `не подходит`, `не этот`, `не надо бронь` go to the LLM follow-up classifier instead of blindly triggering operator or MCP search.
- Updated `followup_intent_classifier.py` with negation intents (`reject_operator`, `reject_phone`, `reject_selected_option`, `reject_similar_options`, `clarify_negation`) and rules: negation should first clarify meaning, not auto-run MCP.
- Updated `scripts/nmbot_mcp_only_sim.py` to demonstrate negation flow: `не подходит` / `не надо бронь` → `negation_to_followup_classifier, no_new_mcp`; `не хочу оператора` → warm refusal without handoff.
- Verification after negation fix: `python3 -m py_compile scripts/chat_tester_bot.py scripts/nmbot_test_agent.py scripts/nmbot_mcp_only_sim.py followup_intent_classifier.py`; `python3 scripts/nmbot_test_agent.py --suite ux_e2e --json` 7/7; `python3 scripts/nmbot_test_agent.py --suite h029 --json` 14/14; `python3 scripts/nmbot_test_agent.py --suite h028 --json` 5/5; simulator passed for `1`, `не подходит`, `не хочу оператора`, `не надо бронь`. Post-test OpenRouter table unchanged: today `$0.98`, week `$2.15`, month `$27.17`, total `$29.60`.
- Tightened negation wording after review: removed heavy phrases like “Сначала уточняю смысл отказа”, “без оператора и без звонка”, and “ничего никуда не передаю”. Simulator now says: “Поняла. Что меняем в подборе — цену, район, срок, отделку или сам ЖК?” and “Хорошо, тогда остаёмся здесь...”. Verification: `ux_e2e` 7/7 and simulator recheck passed; OpenRouter cost unchanged.
- Moved negation client-facing answers from hardcoded text to LLM presenter with code fallback. Added `OvermindClient.explain_negation_followup()` and `_build_negation_response_prompt()`; code still chooses safe intent (`reject_operator`, `reject_phone`, `reject_selected_option`, `reject_similar_options`, `clarify_negation`), but LLM writes the human reply from `NEGATION_CONTEXT` without new MCP search.
- Negation prompt contract includes `ROLE`, `SITUATION`, `NEGATION_CONTEXT`, `GLOBAL_RULES`, `OUTPUT_JSON`; `NEGATION_CONTEXT` passes safe selected option, other options except selected, rejected option names, classifier meta, last question/offer; rules forbid repeating rejected ЖК, asking phone after refusal, listing similar options after refusal, technical phrases, and invented live facts.
- Verification after LLM negation presenter: `python3 -m py_compile scripts/chat_tester_bot.py scripts/nmbot_test_agent.py followup_intent_classifier.py scripts/nmbot_mcp_only_sim.py`; `python3 scripts/nmbot_test_agent.py --suite h029 --json` 15/15; `python3 scripts/nmbot_test_agent.py --suite ux_e2e --json` 7/7; `python3 scripts/nmbot_test_agent.py --suite h028 --json` 5/5; simulator recheck passed for `1`, `не подходит`, `не хочу оператора`, `не надо бронь`. Post-test OpenRouter table: today `$1.01`, week `$2.18`, month `$27.20`, total `$29.63`.
- Moved initial selected-ЖК presentation (`1` / ЖК name) from deterministic `_format_option_response()` to the existing LLM presenter `client.explain_known_option()`. `_format_option_response()` remains fallback only if LLM/style call fails. This targets the manual-review issue where selected card sounded like a field dump (“По цене вижу ориентир...”).
- Tightened `_build_known_option_prompt()` after live review: for primary selection (`CLIENT_INTENT=selected_option_detail`) Irina must not offer operator/contact/booking/availability unless the client explicitly asked live-data details; she should ask whether to compare with other variants or unpack this ЖК deeper. Prompt also forbids unsupported location praise like “отличный район” and overclaims like “любой дизайн-проект”.
- Verification after selected-presenter change: `python3 -m py_compile scripts/chat_tester_bot.py scripts/nmbot_test_agent.py followup_intent_classifier.py scripts/nmbot_mcp_only_sim.py`; `python3 scripts/nmbot_test_agent.py --suite h029 --json` 15/15; `python3 scripts/nmbot_test_agent.py --suite ux_e2e --json` 7/7; `python3 scripts/nmbot_test_agent.py --suite h028 --json` 5/5.
- Manual live dialog review after fix: `студия в пределах МКАД для инвестиций` → `1` now gives a warmer selected ЖК presentation for ЖК «Лучи» without early operator handoff: explains Солнцево, отделка, площади, price from 10.6 mln, 2027 horizon, investment budget entry, then asks “сравнить этот проект с другими вариантами или разобрать его подробнее?”. Follow-ups remained sane: `не подходит` excludes Лучи and offers remaining options; `не хочу оператора` no longer requests contact; `не надо бронь` clarifies without MCP/operator. OpenRouter table after live test: today `$1.31`, week `$2.48`, month `$27.49`, total `$29.93`.
- Aligned `scripts/nmbot_mcp_only_sim.py` with production routing instead of maintaining a second regex bot. Simulator now imports and calls production `_resolve_dialog_intent()` and `_visible_options_from_response()`, builds production-like state (`last_options`, `visible_options`, `selected_option`, `params`, `last_bot_question`, `last_offer_type`, `last_answer_kind`, `selected_option_card_shown_count`), and prints the resolved intent for each turn. It still avoids OpenRouter/MCP follow-up calls: `followup_classifier` is shown as a delegated placeholder.
- Simulator verification covered `второй`, `расскажи подробнее`, `1, можно бронь?`, `1 но дорого`, `не этот`, `не хочу оператора`, `сравнить с другими`, `да` after operator offer, and `ЖК Лучи, этажи есть?`. Results now mirror production routing: word choice → `select_option`, mixed live-data → `operator_for_selected`, mixed semantic/negation → `followup_classifier`, explicit operator refusal → `reject_operator`, operator confirmation → `operator_contact_accept`.
- Verification after simulator alignment: `python3 -m py_compile scripts/chat_tester_bot.py scripts/nmbot_test_agent.py followup_intent_classifier.py scripts/nmbot_mcp_only_sim.py`; `python3 scripts/nmbot_test_agent.py --suite h029 --json` 16/16; `python3 scripts/nmbot_test_agent.py --suite ux_e2e --json` 7/7; `python3 scripts/nmbot_test_agent.py --suite h028 --json` 5/5. OpenRouter cost table after checks: today `$1.49`, week `$2.66`, month `$27.68`, total `$30.12`.
- Live self-dialog review after simulator alignment: scenario `ищу однушку до 15 млн в Москве, желательно с отделкой, для жизни` → `1` → `расскажи подробнее` → `не подходит, хочу дешевле` → `до 12 млн` → `2` → `можно забронировать?`. Good: first answer gave 3 options only, no buttons, no early operator; `не подходит, хочу дешевле` correctly clarified budget first; `до 12 млн` triggered a new search; booking routed to operator/live availability. Bad: selected/detail LLM still hallucinated unsupported `комфорт-класс` for ЖК «Лучи» twice; after user rejected Лучи, the cheaper search returned Лучи again; selected detail question “какой аспект” is too generic; operator handoff is safe but still a bit mechanical.
- Manual score for this self-dialog: about 7/10. Mechanics and routing are much better, but the next architectural fix should target fact-grounding post-check / prompt enforcement for unsupported class/segment, plus exclude rejected ЖК from subsequent refinement searches when possible.
- Verification after self-dialog: `python3 -m py_compile scripts/chat_tester_bot.py scripts/nmbot_test_agent.py followup_intent_classifier.py scripts/nmbot_mcp_only_sim.py`; `h029` 16/16; `ux_e2e` 7/7; `h028` 5/5. OpenRouter cost table after live dialog: today `$1.50`, week `$2.67`, month `$27.69`, total `$30.12`.
- Fixed the two issues found in the live review: unsupported class/segment hallucinations and rejected ЖК reappearing after refinement. Added grounding post-check `_strip_unsupported_complex_claims()` for main search and selected/detail answers, filtered `developer=comfort/business/premium` out of safe facts, and remembered selected ЖК as rejected even when `не подходит, хочу дешевле` first routes to `clarify_negation`.
- Added rejected-option filtering to `_refresh_search_state()` and visible responses so subsequent searches do not show ЖК the client has already rejected. Added name artifact cleanup for nested names like `ЖК «ГК «Варшавские ворота»»` and more natural operator handoff names like `По ЖК «Квартал Мит»...`.
- Verification after grounding/rejected-option fix: `python3 -m py_compile scripts/chat_tester_bot.py scripts/nmbot_test_agent.py followup_intent_classifier.py scripts/nmbot_mcp_only_sim.py`; `python3 scripts/nmbot_test_agent.py --suite h029 --json` 20/20; `python3 scripts/nmbot_test_agent.py --suite ux_e2e --json` 7/7; `python3 scripts/nmbot_test_agent.py --suite h028 --json` 5/5.
- Final live self-dialog after the fix: no `комфорт-класс`/`бизнес-класс` leakage; after `не подходит, хочу дешевле` the bot remembered `ЖК «Лучи»` as rejected; after `до 12 млн` the visible list did not include Лучи and showed `Южные Сады`, `Амурский парк`, `2-й Иртышский`; `можно забронировать?` correctly routed to operator/live availability. OpenRouter table: today `$1.70`, week `$2.87`, month `$27.88`, total `$30.32`.
- Extra family live-dialog after rejected/filter fixes: `ищу двухкомнатную квартиру для семьи до 18 млн...` → `2` → `не этот` → `покажи другие` → `1` → `не хочу с отделкой` → `до 17 млн без отделки` → `1` → `какие этажи есть?` → `не хочу оператора`. Mechanics were good: selected ЖК rejection is remembered, renovation preference changes search, rejected ЖК does not reappear, operator refusal stays in chat, no class leakage. Manual review found wording issues: generic “какой аспект”, live-data CTAs too early, “планировки” overclaim, and mechanical operator handoff.
- Fixed those wording classes architecturally: `_build_known_option_prompt()` now forbids generic “какой аспект”, universal family claims, and live-plan questions unless user asks live details; `prompts/chat_v1.txt` now requires numbered lists for 2+ variants and forbids early live-data CTAs like “наличие конкретных планировок и актуальные цены...”; added post-checks `_strip_unrequested_live_data_cta()`, `_soften_layout_overclaim()`, `_soften_generic_selected_question()`; warmed `_format_operator_handoff_for_option()` to “лучше не гадать... оператор посмотрит...”.
- Verification after final wording pass: `python3 -m py_compile scripts/chat_tester_bot.py scripts/nmbot_test_agent.py followup_intent_classifier.py scripts/nmbot_mcp_only_sim.py`; `python3 scripts/nmbot_test_agent.py --suite h029 --json` 21/21; `python3 scripts/nmbot_test_agent.py --suite ux_e2e --json` 7/7; `python3 scripts/nmbot_test_agent.py --suite h028 --json` 5/5.
- Final live recheck after wording pass: first family answer is numbered and has no early operator; selected/details no class leak and no “планировки и цены” live CTA; operator handoff routes correctly. Manual remaining imperfections: model can still say “актуально” in a broad sense and selected copy can be a bit enthusiastic (“отличный вариант”), but no blocking UX/routing defect found. OpenRouter table: today `$1.80`, week `$2.97`, month `$27.99`, total `$30.42`.
- Implemented first LLM-first dialog state planner in `followup_intent_classifier.py`: new `DIALOG_STATE_PLANNER_PROMPT` and `plan_dialog_state()` return JSON plan (`dialog_action`, `params_delta`, `selected_option_action`, `rejected_options_add`, `visible_options_policy`, `numeric_choice_policy`, `clarification_question`). `scripts/chat_tester_bot.py` applies the plan as a safe state patch via `_apply_dialog_plan_to_state()`; code no longer decides all semantic transitions alone.
- Added numeric-choice safety: `_numeric_choice_policy_from_response()` rejects numeric replies when the visible numbered list cannot be reliably mapped to parsed options. This prevents the old `Ситимикс Новокосино` vs `Ситимикс` mismatch from silently selecting the wrong ЖК. A planner attempt to override rejected numeric policy is now blocked.
- Verification after dialog planner: `python3 -m py_compile scripts/chat_tester_bot.py scripts/nmbot_test_agent.py followup_intent_classifier.py scripts/nmbot_mcp_only_sim.py`; `python3 scripts/nmbot_test_agent.py --suite h029 --json` 26/26; `python3 scripts/nmbot_test_agent.py --suite ux_e2e --json` 7/7; `python3 scripts/nmbot_test_agent.py --suite h028 --json` 5/5.
- Live 3-scenario planner recheck: investment flow now blocks unsafe numeric choice when parser sees an unreliable list and asks for ЖК name / to show options again; if no selected ЖК and user asks `есть бронь?`, planner asks which ЖК instead of using stale selected option. Rejection + search refinement clears/filters selected ЖК correctly. Remaining UX issues: some first answers still lose numbering or visible parsing, and fallback “нет надёжного списка” is safe but less elegant. OpenRouter table after live: today `$0.53`, week `$3.51`, month `$0.53`, total `$30.96`.
- Added structured `visible_options[]` contract to `prompts/chat_v1.txt`: chat LLM now returns `response`, `params`, `visible_options`, `buttons`; `visible_options[]` must contain the exact ЖК shown to the client in numbered order 1/2/3 and only from MCP facts/near. `scripts/chat_tester_bot.py` now stores `chat_meta["_visible_options"]` and uses `_visible_options_from_chat_or_response()` before falling back to text parsing.
- Fixed structured visible-options mapping after live review: LLM can send 0-based `idx`, so `_visible_options_from_chat_meta()` now trusts `name` first and uses idx only as fallback. This restored correct numeric choice for `1/2/3` when LLM returns `idx: 0/1/2`.
- Added planner safety so `numeric_choice_policy=reject` from the LLM planner does not break an already reliable visible list unless the list was cleared, params changed, or the user actually rejected something. This fixed the `покажи другие` case where the planner wanted to reject numbers even though the current visible list was still valid.
- Verification after structured visible-options work: `python3 -m py_compile scripts/chat_tester_bot.py scripts/nmbot_test_agent.py followup_intent_classifier.py scripts/nmbot_mcp_only_sim.py`; `python3 scripts/nmbot_test_agent.py --suite h029 --json` 28/28; `python3 scripts/nmbot_test_agent.py --suite ux_e2e --json` 7/7; `python3 scripts/nmbot_test_agent.py --suite h028 --json` 5/5.
- Targeted live recheck after structured visible-options fix: investment studio search returned 3 structured visible options (`Южные Сады`, `Бусиновский парк`, `Лучи`) with `numeric_policy=accept`; `1` selected `Южные Сады`; after refinement `не подходит, хочу ближе к метро`, the new list returned structured `Ситимикс Новокосино` / `Ситимикс`; `покажи другие` no longer broke numeric choice; `2` selected the visible second option correctly. OpenRouter table: today `$0.71`, week `$3.68`, month `$0.71`, total `$31.13`.
- Added **Prod Deploy/Verification Gate** after user caught that local green tests did not mean real MINION was updated. New rule in `AGENTS.md` and `docs/EXPERIMENTS.md`: every UX/prompt/routing/state/MCP-visible-options change must be deployed/synced to VPS, `novostroy-bot.service` restarted, feature markers checked on VPS, and final verification performed against prod/VPS MINION before saying “готово”. If only local tests ran, report must say: “локально зелёное, prod ещё не проверен”.
- Raised phone capture above LLM/search: text phone numbers and Telegram contacts are now handled by code before any dialog planner/search call. Valid phone is captured when there is operator/contact context (`awaiting_phone`, `operator_context`, selected ЖК, or recent operator/details offer); without context the bot asks which ЖК/request to attach. Budget fragments like `до 200к` are not treated as phones.
- Added phone helper tests in `ux_e2e`: `phone_capture_is_code_level_and_requires_context` verifies `+7 999 123-45-67`, `89991234567`, invalid `до 200к`, context detection after operator offer, and no-context clarification.
- Verification after phone capture fix: `python3 -m py_compile scripts/chat_tester_bot.py scripts/nmbot_test_agent.py followup_intent_classifier.py scripts/nmbot_mcp_only_sim.py`; `python3 scripts/nmbot_test_agent.py --suite ux_e2e --json` 8/8; `python3 scripts/nmbot_test_agent.py --suite h029 --json` 28/28; `python3 scripts/nmbot_test_agent.py --suite h028 --json` 5/5.
- Deployed phone capture fix to VPS `/home/neiro/novostroy-bot`: backup `backups/deploy-20260701-084259`, synced `scripts/chat_tester_bot.py`, remote `py_compile` passed, `novostroy-bot.service` restarted and active. Prod smoke: `_extract_phone_from_text('+7 999 123-45-67') -> +79991234567`, operator context is true, `да` after “Оставите номер?” routes to `operator_contact_accept`, and `до 200к` is not a phone. OpenRouter table after prod smoke: today `$0.74`, week `$3.71`, month `$0.74`, total `$31.17`.
- Fixed selected-investment UX complaint from prod: after user chooses a ЖК for investment, final CTA should lead to operator/live lot check, not keep asking to compare/discuss prices. Added selected-investment CTA postcheck that replaces questions like “сравнить этот проект / разобрать цены?” with “Хотите оставить номер — оператор проверит по <ЖК> актуальные квартиры, цены входа и условия покупки?”. Added paragraph spacing so the final question is separated by a blank line. Also send Telegram `typing` action immediately after text is accepted, before follow-up/LLM routing, so numeric choices do not look frozen while the LLM presenter works.
- Verification after selected-investment CTA/spacing fix: `py_compile`; `ux_e2e` 9/9; `h029` 28/28; `h028` 5/5. Deployed to VPS: backup `backups/deploy-20260701-085346`, remote `py_compile` passed, `novostroy-bot.service` restarted and active. Prod smoke confirmed CTA rewrite and paragraph split. OpenRouter table after prod smoke: today `$0.75`, week `$3.72`, month `$0.75`, total `$31.17`.

## 2026-07-02 — MVP stage orchestrator

- Started after user asked: `делай по шагам`.
- Loaded `planning-with-files`, ran session catchup for `/tmp/opencode-run-nmbot/project` (no output), read existing planning files and `docs/IRINA_DIALOGUE_MAP_V1.md`.
- Updated planning files with MVP stage orchestrator task. Current goal: implement only `first_list`, `selected_object`, `operator_handoff`, keep nonstandard cases on existing fallback/freeform.
- Implemented local MVP stage presenter in `scripts/chat_tester_bot.py`: `NMBOT_STAGE_PRESENTER=1` default on; reason-layer moved back to opt-in lab mode; deterministic first-list and selected-object presenters added.
- Local verification after latest patch: `python3 -m py_compile scripts/chat_tester_bot.py` passed. Smoke for family first-list and selected `ЖК «Лучи»` passed: no `сдача/готовность`, no `верхняя точка`, no `MCP`/`по данным`/`в базе`, prices use commas, and both answers have exactly one final question.
- Deployed MVP stage presenter to VPS `/home/neiro/novostroy-bot`: backup `backups/deploy-20260702-183048`, remote `py_compile` passed, `novostroy-bot.service` restarted and active with PID `2107648`.
- Prod smoke confirmed `STAGE_PRESENTER_ENABLED=True`, `REASON_LAYER_ENABLED=False`, deterministic family first-list and selected `ЖК «Лучи»` render cleanly with one final question and no banned technical phrases. OpenRouter cost after deploy: today `$1.22`, week `$6.59`, month `$3.62`, total `$34.04`.

## 2026-07-03 — Scenario-aware first_list style

- Started after user rejected dry first-list presentations for family scenarios and asked to do the fix step by step with verification.
- Updated `task_plan.md` with the scenario-aware first-list plan and constraints: no hallucinations, max 3 ЖК, exactly one next question, local green is not prod green.
- Patched `scripts/chat_tester_bot.py` normalizer to preserve more family infrastructure aliases from MCP/search payloads: schools, kindergartens, parks/forest/embankment, clinics/polyclinic/pharmacies, yards/playgrounds/car-free yards, shops/services.
- Added scenario-aware fact ordering in `_stage_option_fact_parts(option, scenario)`: family facts now prioritize schools, kindergartens, parks, clinics, yards before price/readiness; investment and fast-move keep their own priorities.
- Improved family fallback benefits so they use concrete factual text like `2 школы`, `4 детских сада`, `Мещерский парк`, instead of generic “зелёная зона” phrasing.
- Added H029 tests for infrastructure alias preservation and family first-list rendering with real infrastructure; tests also assert max 3 options and exactly one final question.
- Fixed planner safety guards in `_apply_dialog_plan_to_state()` without moving semantics into regex code: the LLM planner still decides rejection/selection, while code only blocks unsafe application when numeric choice is already rejected or when rejected-option updates do not accompany an actual list/search/state change.
- Local verification: `python3 -m py_compile scripts/chat_tester_bot.py scripts/nmbot_test_agent.py` passed; `python3 scripts/nmbot_test_agent.py --suite h029 --json` passed 31/31.
- Prod/VPS deploy and smoke were not run in this step, so the change is locally green only.
- Deployed the scenario-aware first_list fix to VPS `/home/neiro/novostroy-bot`: backup `backups/deploy-20260703-122246`, synced `scripts/chat_tester_bot.py` and `scripts/nmbot_test_agent.py`, remote `py_compile` passed, and `novostroy-bot.service` restarted cleanly.
- VPS smoke after deploy: `python3 scripts/nmbot_test_agent.py --suite h029 --json` passed 31/31 on the VPS, including family infrastructure aliases, family first-list rendering, and numeric-choice guard tests.
- Post-deploy cost snapshot on VPS: today `$1.20`, week `$8.39`, month `$5.41`, total `$35.84`.

## 2026-07-03 — Advice/operator follow-up fix

- Started after live Telegram review showed two follow-up regressions: `а ты что посоветуешь?` / `твой совет какой` repeated a dry comparison list instead of a recommendation, and `как связаться с оператором?` asked the user to choose first/second/third instead of asking for phone with current context.
- Patched `followup_intent_classifier.py`: added `recommend_options` to allowed intents/actions and prompt rules; direct operator/contact phrases now route to `operator_live_check` even when `selected_option` is empty, so code can pass the current visible list and criteria.
- Patched `scripts/chat_tester_bot.py`: added `_render_stage_recommendation()` and `_recommendation_score()` for deterministic factual recommendation after the LLM planner chooses `recommend_options`; added `_format_operator_handoff_for_context()` and `operator_for_context` route for direct operator request without a selected ЖК.
- Added H029 regression tests in `scripts/nmbot_test_agent.py`: family advice recommends the strongest factual option and does not say “Давайте сравним”; operator-without-selected asks for phone and includes current option context instead of asking first/second/third.
- Local verification: `python3 -m py_compile scripts/chat_tester_bot.py scripts/nmbot_test_agent.py followup_intent_classifier.py` passed; `python3 scripts/nmbot_test_agent.py --suite h029 --json` passed 33/33.
- Deployed advice/operator follow-up fix to VPS `/home/neiro/novostroy-bot`: backup `backups/deploy-20260703-124828`; corrected sync paths for `scripts/chat_tester_bot.py`, `scripts/nmbot_test_agent.py`, and `followup_intent_classifier.py`; remote `py_compile` passed; `novostroy-bot.service` restarted and active with PID `2296210`.
- VPS smoke after corrected deploy: `python3 scripts/nmbot_test_agent.py --suite h029 --json` passed 33/33, including new `family_advice_recommends_best_option_not_compare_loop` and `operator_without_selected_asks_phone_with_current_context` tests.
- Post-deploy cost snapshot on VPS: today `$1.34`, week `$8.53`, month `$5.55`, total `$35.98`.

## 2026-07-03 — Bot-visible history command

- Started after user asked for a bot-visible history/debug command that shows both client requests and bot answers plus the full search cycle/MCP payload.
- Implemented `/history` and `/hisotry` in `scripts/chat_tester_bot.py`; `/hisotry` is intentionally supported as the typo alias requested by the user.
- The command reads recent `user_message` entries for the current Telegram user from `logs/dialogs-YYYY-MM-DD.jsonl` and formats: `Вы`, `Бот`, `intent`, `plan`, `MCP/search_response`, `buttons`, `cost`.
- Added safe compacting and Telegram chunking so long MCP/search traces do not exceed message limits.
- Updated docs: `docs/CHANGELOG.md` and `docs/BOT_ARCHITECTURE.md`; updated `task_plan.md` with the dedicated history-command plan.
- Added H029 regression tests for history formatting, MCP/search fact visibility, Telegram chunking, and registration of both `/history` and `/hisotry`.
- Local verification: `python3 -m py_compile scripts/chat_tester_bot.py scripts/nmbot_test_agent.py followup_intent_classifier.py` passed; `python3 scripts/nmbot_test_agent.py --suite h029 --json` passed 36/36.
- Deployed to VPS `/home/neiro/novostroy-bot`: backup `backups/deploy-20260703-131729`, synced runtime/tests/docs, remote `py_compile` passed, `novostroy-bot.service` restarted and active with PID `2307297`.
- VPS smoke after deploy: `python3 scripts/nmbot_test_agent.py --suite h029 --json` passed 36/36, including new history command tests.
- Post-deploy cost snapshot on VPS: today `$1.59`, week `$8.78`, month `$5.80`, total `$36.23`.
- Git commit and push are still pending at this checkpoint.

## 2026-07-03 — Public project overview service

- User clarified that an existing public web service already runs at `http://193.107.155.236:8765/mpn-quality-7f3a9c/index.html`, so the correct solution is not a new service/port but one shared public entry point.
- Confirmed on VPS: `/usr/bin/python3 -m http.server 8765 --bind 0.0.0.0 --directory /home/neiro/public` serves the existing MPN dashboard.
- Added `scripts/build_public_overview.py`: a static allow-list builder that creates `public_site/index.html`, `public_site/nmbot-project-7f3a9c/index.html`, and `public_site/nmbot-project-7f3a9c/map.html` from safe docs/prompts only.
- Published to VPS public root:
  - `http://193.107.155.236:8765/index.html` — shared service list with MPN + NMBOT.
  - `http://193.107.155.236:8765/nmbot-project-7f3a9c/index.html` — NMBOT project overview.
  - `http://193.107.155.236:8765/nmbot-project-7f3a9c/map.html` — full scenario map.
- Backup before publish: public backup `public-20260703-135202`, repo backup `deploy-20260703-135202`.
- Verification: local build + HTML parser passed; remote HTML parser and marker checks passed with `REMOTE_PUBLIC_OVERVIEW_OK`; public webfetch confirmed both index and NMBOT pages are accessible.
- Updated `docs/CHANGELOG.md` and `docs/BOT_ARCHITECTURE.md` to document the public overview.
- Git commit/push pending from real VPS repo.
