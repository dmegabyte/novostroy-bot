# 03 — Metadata note

Evidence IDs: `leak.e7`, `leak.e8`, `leak.e9`.

- `leak.e7`: The metadata field described an internal planning reason, not a safe
  client presentation contract.
- `leak.e8`: Operator consent/contact state was not dependent on showing this raw
  reason.
- `leak.e9`: The verified safe client context for this branch was the selected
  project name plus one user-facing question.
